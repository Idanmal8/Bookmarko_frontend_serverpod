import 'package:flutter/material.dart';

class ServicesEditScreen extends StatelessWidget {
  const ServicesEditScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('Services Edit Screen'),
    );
  }
}